# UniOne region: "eu" (default) or "us"
UNIONE_REGION=eu

# Server configuration
PORT=3001
LOG_LEVEL=info

# Default provider for Universal API v1 (resend, unione)
MAIL_PROVIDER=resend

# Note: API keys are provided per-request:
# - v3 API: Basic Auth with provider:apikey format
#   Example: Authorization: Basic base64(resend:re_xxxxxxxxxxxx)
# - v1 API: Bearer token or X-API-Key header
#   Example: Authorization: Bearer re_xxxxxxxxxxxx
