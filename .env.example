# UniOne region: "us" (default) or "eu"
UNIONE_REGION=us

# Server configuration
PORT=4050
LOG_LEVEL=info

# Note: API keys are now provided per-request via Basic Auth
# Format: Authorization: Basic base64(provider:apikey)
# Examples:
#   resend:re_xxxxxxxxxxxx
#   unione:your-unione-api-key
